/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com'], // Add any other image domains you might use
  },
  reactStrictMode: true,
}

export default nextConfig 